---
const { title = "IsoTrack · Panel protegido", subtitle = "Acceso seguro y sin fricción", description = "Centraliza la autenticación y los documentos iniciales en un mismo lugar, con feedback claro del estado de la sesión." } = Astro.props;
---
<section class="card-surface p-8 space-y-4">
  <div class="flex flex-wrap items-start justify-between gap-4">
    <div class="space-y-2">
      <p class="badge bg-primary/10 text-primary">Entorno Fase 1.4</p>
      <h1 class="text-3xl font-bold text-ink">{title}</h1>
      <p class="text-slate-600 max-w-3xl">{description}</p>
    </div>
    <div class="text-right space-y-2">
      <p class="text-xs uppercase text-slate-500">Experiencia</p>
      <p class="font-semibold text-ink">{subtitle}</p>
      <p class="text-xs text-slate-500 max-w-xs">
        Indicadores de sesión en vivo, datos de empresa y acceso protegido al repositorio documental.
      </p>
    </div>
  </div>

  <div class="grid sm:grid-cols-3 gap-4 text-sm text-slate-600">
    <div class="flex items-center gap-3 rounded-xl border border-slate-200 px-4 py-3 bg-slate-50/70">
      <div class="size-10 rounded-full bg-primary/10 text-primary grid place-items-center font-semibold">1</div>
      <div>
        <p class="font-semibold text-ink">Valida sesión</p>
        <p class="text-xs text-slate-500">Revisa tokens y expira de forma segura.</p>
      </div>
    </div>
    <div class="flex items-center gap-3 rounded-xl border border-slate-200 px-4 py-3 bg-slate-50/70">
      <div class="size-10 rounded-full bg-primary/10 text-primary grid place-items-center font-semibold">2</div>
      <div>
        <p class="font-semibold text-ink">Saluda y contextualiza</p>
        <p class="text-xs text-slate-500">Perfil enriquecido y datos de empresa activos.</p>
      </div>
    </div>
    <div class="flex items-center gap-3 rounded-xl border border-slate-200 px-4 py-3 bg-slate-50/70">
      <div class="size-10 rounded-full bg-primary/10 text-primary grid place-items-center font-semibold">3</div>
      <div>
        <p class="font-semibold text-ink">Sincroniza documentos</p>
        <p class="text-xs text-slate-500">Listado y detalle del primer recurso disponible.</p>
      </div>
    </div>
  </div>
</section>
