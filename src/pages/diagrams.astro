---
import Layout from "../layouts/Layout.astro";
import DiagramsPage from "../components/diagrams/DiagramsPage";
import { API_URL } from "../consts";

const diagramsEndpoint = `${API_URL}/diagrams`;
const linksEndpoint = `${API_URL}/diagrams`;
---

<Layout title="IsoTrack · Diagramas" description="Editor visual de organigramas y flujos">
  <DiagramsPage diagramsEndpoint={diagramsEndpoint} linksEndpoint={linksEndpoint} client:load />
</Layout>
